import React from "react";

const BackgroundSection = () => {
    return (
        <div className="absolute inset-0 bg-neutral-100 dark:bg-neutral-800/30 -z-10" />
    );
};

export default BackgroundSection; 